import { Content } from "next/font/google";
import { NotificationRoot } from "./NotificationRoot";

export const Notification = {
    Root: NotificationRoot,
    Icon: NotificationRoot,
    Content: NotificationRoot,
    Actions: NotificationRoot,
}